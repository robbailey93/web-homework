import React, { Component } from 'react'

class UserList extends Component {
  constructor (props) {
    super(props)
    this.state = { profile: {} }
  }

  render () {
    return (
      <React.Fragment>
        
      </React.Fragment>
    )
  }
}

export default UserList
